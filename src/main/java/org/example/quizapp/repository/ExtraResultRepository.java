package org.example.quizapp.repository;

import org.example.quizapp.entity.ExtraResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExtraResultRepository extends JpaRepository<ExtraResult, Long> {
}
